<div>
  <div class="container">

    <div class="jumbotron">

      <div class="row" id="burg">
        <header class="col-md-12" id="header"><h1>EAT-DA-BURGER</h1></header>
      </div>



      <div class="row">
        <div class="col-xs-12">
          <div style="border-style:solid !important;">
            <h2>Add a Burger</h2>
            {{! <form class="create-update-form" action="/burger/create" method="POST"> }}
            <form class="create-update-form" action="/" method="POST", id= "form1">

            <div class="form-group" id="formText">
              <label for="ca">Burger Name:</label>
              <input type="text" id="ca" name="burger_name">
            </div>

            <button type="submit" class="btn btn-lg btn-primary" style="background-color:orange;"><h4><span class="glyphicon glyphicon-list-alt"></span>Add burger</h4></button>
            </form>
          </div>
        </div>

      </div>

          <div class="container">
            <div class="row">
              <div class="col-xs-12">
              </div>
            </div>

          </div>


          <div class="container">
            <div class="row">
              <div class="col-xs-12">
              </div>
            </div>
          </div> </div>
          <div class="row">
            <div class="col-xs-6">
              <h2>Burgers</h2>

              <ul>
                {{#each burgers}}
                  {{#unless this.devoured}}
                    <li>
                     {{id}} {{burger_name}}

                      <form action="/{{id}}?_method=PUT" method="POST">
                      <input type="hidden" name="devoured" value="true">
                      <button type="submit" style="color:#f37320;">Submit!</button>
                      </form>


                    </li>
                  {{/unless}}
                {{/each}}
              </ul>
            </div>
            <div class="col-xs-6">
              <h2>Eaten Burgers</h2>
              <div>
                {{#each burgers}}
                  {{#if this.devoured}}
                    <p>
                      {{this.burger_name}}

                      {{! <form action="/urgers/update?_method=PUT" method="POST"> }}
                      {{! <input type="hidden" name="Id" value={{this.Id}\} id= "input_devoured"> }}
                      {{! <button type="submit">Submit!</button> }}
                      {{! </form> }}

                      {{!-- {{> burgers/delete-burger}} --}}
                    </p>
                  {{/if}}
                {{/each}}
              </div>
            </div>
          </div>
          <script type="text/javascript">
          </script>
